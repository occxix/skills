# Claude Code Project Instructions

Use the nearest authoritative `CLAUDE.md` for the repo.

## Priority

1. `.claude/CLAUDE.md` if it exists or can be created for this project
2. Root `CLAUDE.md` only when that is the repo's established convention

## Writing rule

- Keep the file project-specific.
- Put stack rules, guardrails, build commands, and repo conventions here.
- Do not duplicate the same content across both files unless the repo intentionally layers them.
