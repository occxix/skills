# GitHub Copilot CLI Project Instructions

Use the repo instruction file that best matches the project convention.

## Priority

1. `.github/copilot-instructions.md`
2. `AGENTS.md` if the repo uses agent-style instructions
3. Other repo instruction files only when the project already relies on them

## Writing rule

- Keep the content short and repo-specific.
- Align Copilot instructions with the repo's other AI instruction files.
